[pytest]
# Import modules from the repo root (ingest.py, rag.py) without installing a package.
pythonpath = .
testpaths = tests

markers =
    slow: parses the full 253-page Act; excluded from the default fast run
